@model  List<CarsSystem.WebClient.MVC.Areas.Administration.Models.Filter.FilterViewModel>

@{
    WebGrid grid = new WebGrid(Model, rowsPerPage: 5);
}

@grid.GetHtml(
    tableStyle: "table table-striped table-hover form-group",
    headerStyle: "text-right",
    columns: grid.Columns(
        grid.Column(columnName: "Customer", format: (item) => Html.ActionLink(((string)item.CustomerOfVehicle), "Details", "Users", new { id = item.CustomerId }, null)),
        grid.Column(columnName: "Manufacturer", format: (item) => Html.ActionLink(((string)item.Manufacturer), "Details", "Cars", new { id = item.CarId }, null)),
        grid.Column("Manufacturer", "Manufacturer"),
        grid.Column("Model", "Model"),
        grid.Column("RegistrationNumber", "Registration number"),
        grid.Column("VINNumber", "VIN number"),
        grid.Column("ExpirationDate", "Valid until", format: (item) => string.Format("{0:dd/MM/yyyy}", item.ExpirationDate)),
        grid.Column("UserNotificationMessage", "Is user notified for expiring via e-mail?")
      ))
